import React, { FC } from 'react';

interface IHeaderProps {

}

const Header: FC<IHeaderProps> = (props) => {
  return (
    <header className='fixed bg-white shadow'>    
        <div >

        </div>
    </header>
  );
}

export default Header;
